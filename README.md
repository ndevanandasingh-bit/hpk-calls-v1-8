# HPK Calls V1.8 - Mobile GitHub Upload Edition

Upload the files in this folder to the ROOT of your GitHub repository, one file at a time if necessary.

Required Render settings:
- Runtime/Language: Node
- Build Command: echo "No build required"
- Start Command: node server.js

After deployment test:
- /api/health
- then open the root URL to launch HPK Calls.
